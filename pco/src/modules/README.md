# PCO Module Scaffolds

Each module should stay isolated so compliance/admin features can evolve without touching the live dispatch app.

Suggested modules:

- `compliance/`
- `vehicles/`
- `company/`
- `billing/`
- `payouts/`
- `audit/`
