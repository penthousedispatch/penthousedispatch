{
  "installCommand": "cd project && npm ci",
  "buildCommand": "cd project && npm run build",
  "outputDirectory": "project/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
